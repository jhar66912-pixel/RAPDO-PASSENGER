import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rapdo.passenger',
  appName: 'RAPDO',
  webDir: 'dist',
  // @ts-ignore
  bundledWebRuntime: false
};

export default config;
